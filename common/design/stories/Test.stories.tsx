import type { Meta } from "@storybook/react";

export const Test = () => {
  return <h1 className="text-blue-500 text-sm">Test</h1>;
};

export default {
  title: "Test",
  component: Test,
} as Meta;
