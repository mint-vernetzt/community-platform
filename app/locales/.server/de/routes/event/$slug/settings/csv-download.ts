export const locale = {
  error: {
    notFound: "Participants not found",
    notPrivileged: "Not privileged",
    badRequest: {
      depth: "search parameter - depth = full || single - must be provided.",
      type: "search parameter - type = participants || waitingList - must be provided.",
    },
  },
} as const;
